# 高迪书法字库预处理工具 版本历史

## 2026-04-14

### Bug修复

- **修复竖向切割线拖动无效问题**（页面1-切割布局）
  - 问题：Shift+双击添加竖向切割线后，拖动移动时位置没有真正更新，横向切割线不跟随变化，切割结果仍是移动前的位置
  - 原因：`handleMouseMove` 中拖动竖线时只更新了 `state.verticalLines` 的值，但未同步更新 `stripHorizontalLines` 中对应列的 `x_start`/`x_end`
  - 修复：拖动竖线时同步更新相邻两列的边界值
  - 涉及文件：`static/js/layout.js`

### 功能改进

- **导出后自动清理中间文件**（页面4-标注出图）
  - 导出白底黑字后自动清理切割原始文件（char_XXXX.png）和缩放校正目录（scaled/），只保留最终导出目录
  - 清理旧导出目录，仅保留本次导出
  - 新增后端API：`/api/cleanup_intermediate`
  - 涉及文件：`app.py`、`static/js/annotate.js`

- **右键删除错误字**（页面4-标注出图）
  - 右键点击字符卡片弹出确认框，确认后标记为已删除（半透明+不可操作）
  - 标注时自动跳过已删除卡片，字符计数仅统计有效卡片
  - 涉及文件：`static/js/annotate.js`、`templates/annotate.html`

- **支持CJK扩展区字符**（页面4-标注出图）
  - 修复 `getUtfCode`：`charCodeAt(0)` → `codePointAt(0)`，正确显示扩展区编码（如 U+249B2）
  - 修复 `removePunctuation`：正则扩展覆盖 CJK 扩展A（U+3400-4DBF）和扩展B-F（U+20000-2FA1F），使用 `u` 标志匹配补充平面
  - 移除输入框 `maxlength="1"` 限制（扩展区字符需2个UTF-16码元），改用 `Array.from()` 提取第一个完整码点
  - 字符计数使用 `Array.from()` 替代 `.length`，正确统计包含代理对的文本
  - 文本拆分使用 `Array.from(text)` 替代 `text.split('')`
  - 涉及文件：`static/js/annotate.js`

- **图片命名规范**（页面4-标注出图）
  - BMP字符（U+0000–FFFF）：`uniXXXX` 格式，如 `uni4E00.png`
  - 扩展区字符（U+10000+）：`uXXXXX` 格式，如 `u249B2.png`
  - 符合 FontLab 字形命名规范
  - 涉及文件：`app.py`

## 2026-09-09

### 功能改进

- **`/api/list_sessions` 加 `exports` 字段**（对接 ai-font-tool）
  - 背景：ai-font-tool 训练时需要知道每个 session 有几个历史导出版本、对应路径、文件数。手动 `ls exported/` 不规范也不便脚本消费
  - 改动：新增 `_scan_session_exports(h)` 辅助函数，`list_sessions` 返回里每个 session 多 `exports: [{ts, path, png_count, csv_rows, created_at}, ...]`，按 mtime 倒序
  - 数据来源 = 磁盘扫描（不是 cutting.json 记录）：用户手动删 export 目录后 API 自动不再列出该版本，符合「手动删 = 不需要了」语义；不维护「导出历史数据库」避免与现实脱节
  - 过滤规则：只算 `os.path.isdir` 子目录 + 子目录名匹配 `^\d{8}_\d{6}$` 时间戳格式 + 子目录里有 PNG 才算「完整导出」（自动过滤 `backup/` 等非时间戳目录）
  - **前端不消费**——专门给 ai-font-tool-dev 训练页用，前端零改动
  - 涉及文件：`app.py`（list_sessions + _scan_session_exports）

- **删除 toolbar「导出 FontLab CSV」按钮**（页面4-标注出图）
  - 原因：「导出训练包」原子化三件套（PNG + CSV + source_map）后，CSV 已随每次导出自动生成——独立的「导出 FontLab CSV」按钮成了重复入口
  - 后端 `/api/export_csv` 接口**保留**（备用 / 外部脚本可能用），仅删前端按钮 + JS 引用
  - 涉及文件：`templates/annotate.html`（按钮 HTML + `.btn-csv` CSS）、`static/js/annotate.js`（元素字典 + init + exportCSV 函数 + disabled 逻辑）

- **「导出白底黑字」按钮更名「导出训练包」**（页面4-标注出图）
  - 原因：原子化导出（PNG + CSV + source_map 三件套）后，按钮只描述「白底黑字」已不准确——实际产物是给 FontLab/AI 训练工具用的「训练包」，白底黑字只是其中一个属性
  - 涉及文件：`templates/annotate.html`、`static/js/annotate.js`

- **「导出训练包」不再自动清理过程图**（页面4-标注出图）
  - 旧行为：导出完成自动删 cutting_output + scaled，节省磁盘但无法回溯
  - 新行为：保留过程图（cutting_output/scaled/ocr_tasks），便于回溯错误 / 重新导出。需要清盘时点 toolbar 新增的「清理过程图」按钮
  - 清理按钮：莫兰迪浅珊瑚色（温和警示），点击后**一次 confirm 弹窗**列删除清单（cutting_output/scaled/ocr_tasks），点取消则不执行；后端 `/api/cleanup_intermediate` 加 `keep_exported=True` 参数只清过程目录、不动 exported/
  - 涉及文件：`app.py`（cleanup_intermediate）、`templates/annotate.html`（按钮 + CSS）、`static/js/annotate.js`（cleanupIntermediate 函数 + 移除导出后自动清理调用）

### Bug修复

- **「导出白底黑字」原子化三件套（PNG + CSV + source_map 同子目录）**（页面4-标注出图）
  - 现象：原「导出白底黑字」只生成 PNG（写到 `exported/<时间戳>/*.png`），CSV 与 source_map 需另点一次「导出 FontLab CSV」按钮才会生成——两者时间戳不一致，PNG 与 CSV 可能错位；老 CSV 留 `exported/` 根目录，容易混淆
  - 修复：`export_annotated` worker 在写完 PNG 后**累积 CSV 行 + source_map**，所有 PNG 写完后在**同一时间戳子目录**落 `fontlab_<ts>.csv` + `fontlab_<ts>.source_map.json`，与 PNG 原子化同时生成。`/api/export_progress` 回 `csv_path`/`mapping_path`/`csv_row_count`，前端 toast 直接显示「导出完成：319 张，CSV 319 行」
  - 顺手修了 worker 源文件查找 bug：删卡后 scaled 序号与 index 不再一一对应，原代码用 `scaled_{idx:04d}.png` 推断会漏文件；改成「优先按 ann.filename 在 `scaled/` 子目录里查」
  - 涉及文件：`app.py`（export_annotated + export_progress）、`static/js/annotate.js`（toast 提示 CSV 行数）

- **修复导出 CSV 同字样本同名覆盖**（页面4-标注出图）
  - 现象：导出 CSV 319 行只有 152 个唯一 filename，25 个「的」全是 `uni7684.png`、13 个「中」全是 `uni4E2D.png`——与图片导出的实际文件对不上（图片导出有 `_01/_02` 后缀，CSV 没有），下游训练时同名样本互相覆盖
  - 原因：`/api/export_csv` 只按码点拼 `uniXXXX.png`，没做重复后缀处理；而 `/api/export_annotated`（图片导出）用 `char_counts` 做了——两处命名规则不一致
  - 修复：`export_csv` 加同字 `_01/_02` 计数去重（与图片导出同一套规则，保证 CSV 文件名 = 图片实际文件名）；注解带源文件名（`scaled_*.png`/`char_*.png`）时另输出同时间戳 `.source_map.json`（{源文件名: 导出名}），供下游交叉验证
  - 涉及文件：`app.py`（export_csv）

- **修复 /annotate 右键删除后卡片刷新仍出现**（页面4-标注出图）
  - 现象：在 /annotate 右键删除某张卡（如 scaled_0314.png），磁盘 scaled/ 文件与 ocr_annotations.json 记录都没了，但刷新后该卡仍被加载出来（图片还是破的）
  - 原因：一个「字符」在会话 JSON 里有两份条目 + 两份文件——`characters`（filename=char_XXXX.png → cutting_output/）与 `scaled_characters`（processed_filename=scaled_YYYY.png → scaled/）。`/api/delete_characters` 只删了磁盘文件并更新 `characters` 字段，**从没动 `scaled_characters`**，且按 `filename` 匹配不到 /annotate 发的缩放名。而 /annotate 页面从 `/api/get_scaled_results`（读 `scaled_characters`）列卡片 → 条目还在，刷新就复活。原始切图 char_XXXX.png 也未删，重跑缩放会再生成同张卡
  - 修复：`/api/delete_characters` 改为**级联删除**——用 `scaled_characters.original_filename` 建 原始名 ↔ 缩放名 双向映射，无论从 /adjust（原始名）还是 /annotate（缩放名）删，都把两侧文件 + `characters`/`scaled_characters` 条目一起清掉；并同步删 ocr_annotations.json 里对应 key（新增 `_delete_ocr_annotations_by_keys` 辅助，与写标注同一把锁）
  - 涉及文件：`app.py`（delete_characters + 新辅助函数）

- **修复 /annotate 右键「打开图片位置」与卡片图不一致**（页面4-标注出图）
  - 现象：右键卡片选「打开图片位置」，资源管理器里高亮的文件与卡片上看到的图不是同一张图（同一文件名，但图内容不一致）
  - 原因：`/output/<path:filename>` 路由设了 `Cache-Control: max-age=31536000, immutable` 强缓存 1 年。`scale.js:287` 在预览图加 `?t=Date.now()` 绕开缓存，但 `annotate.js:452` 没加——`process_scale` / `save_scaled` 覆盖磁盘上 `scaled_*.png` 后，浏览器仍展示旧缓存图；右键「打开图片位置」却是新文件，导致「图与文件对不上」
  - 修复：`annotate.js` 图片 URL 同样加 `?t=Date.now()`（与 `scale.js:287` 同款做法），保证浏览器永远拿到磁盘上的最新内容
  - 涉及文件：`static/js/annotate.js`、`templates/annotate.html`（JS 版本号 v17 → v18）

## 2026-09-12

### Bug修复

- **/annotate 搜索栏的计数与 ✕ 溢出框外、压在「字符数/卡片数」上**（页面4-标注出图）
  - 现象：并入工具栏后 `.char-count-info` 与 `.card-search-bar` 视觉重叠——搜索栏右侧的计数（「N 张」）和 ✕ 清空按钮跑到了框外面，正好盖住隔壁的「字符数: 0 / 卡片数: 741」
  - **根因：表单控件的 `min-width` 默认是 `auto`，会解析成「固有宽度」**。<input> 没写 `size` 就按默认 `size=20` 算 ≈ 203px，于是搜索栏的内容根（🔍图标 + 输入框 + ? + 计数 + ✕）不低于 **350px**；而工具栏里栏宽被 `max-width: 280px` 压住 → 内容溢出约 **70px**，冒出去的部分压在邻居身上
  - **关键：这不是「字符数该往右挪」能解决的问题**。单纯给 `.char-count-info` 加左外边距，只会让溢出的那 70px 换个位置继续压别的东西，而且白白变宽、会把工具栏顶到第 3 行。要修的是「让搜索栏装得下自己的内容」
  - 修复①：`.card-search-bar input { min-width: 0 }` —— 允许输入框收缩到固有宽度以下，内容就老实呆在框里了
  - 修复②：顺带把搜索计数改成**只在过滤时显示**。不搜索时它显示的是「N 张」= 卡片总数，而同一行的 `.char-count-info` 已经写着「卡片数: N」，两处重复；隐藏后省下 58px（50px min-width + 8px gap），输入框从 133px 涨到 **191px**，占位符才完整显示得下。因为撑宽本身被 `max-width` 挡着，**隐藏计数不会引起工具栏换行**（实测三个状态工具栏都是 109px）
  - 修复③：`updateSearchCount()` 只在用户输入时才被调用，所以计数元素初始态在 HTML 里写死 `display: none`，否则页面刚加载会先露出一个无意义的「0/0」
  - **验证**（Playwright 实测四态，全部无溢出）：空闲 → 输入框 191px、计数隐藏、溢出 0；搜索「中」→ 命中 1 张、显示 1/741、输入框自动收到 133px、溢出 0；「1-50」→ 50/741；Esc → 计数隐藏、输入框回到 191px。四态工具栏均 109px，与字符数的间隙恒为 10px（= 工具栏 gap）；占位符文字实测宽 148px ≤ 可用 169px，完整可见
  - 涉及文件：`templates/annotate.html`（input 的 min-width、计数元素初始态）、`static/js/annotate.js`（`updateSearchCount`）；annotate.js 版本号 v31 → v32

### 功能改进

- **/annotate 把操作栏与搜索栏全部并入工具栏，卡片区多出 84px**（页面4-标注出图）
  - 需求：`.action-buttons`（OCR 阈值/进度条 + 字符计数）和 `.card-search-bar` 的内容能不能一起塞进 `.toolbar`，尽量把空间让给字符卡片
  - **先算账再动手**（1280×900、741 张卡实测）：改动前工具栏 109 + 输入区 133 + 操作栏 38，网格容器顶 315、高 585，其中搜索栏 sticky 常驻在滚动区内部再吃掉 46 → **卡片净高 539px**
  - **改动后**：工具栏**仍是 109px（没有多出一行）**，操作栏整行消失，网格容器顶 315→277、高 585→623，搜索栏已不在滚动区内 → **卡片净高 623px**，**+84px（+15.6%）**。1600/1440/1366 同样是 +84px；1200 因工具栏第 1 行的按钮自己换行属 +41px（仍净赚）
  - **关键发现：工具栏第 2 行左侧本来就空着约 990px** —— `.mode-toggle` 的 `margin-left: auto` 把「繁简切换 + 输入框显示开关」顶到最右，而第 2 行再没有别的元素去填这段空白。三块塞进去正好填这个空位，**因此不多占一行**——这正是这个改动能「纯赚 84px」的原因
  - **搜索栏不再需要 `position: sticky`**：搬进工具栏后它本身就在滚动容器之外，「滚动时始终可见」由布局天然保证（实测滚动 800px 后搜索栏位置纹丝不动）。顺带避开了上一轮踩到的「sticky 元素上外边距失效」那个坑；改为 `position: relative` 以继续给 `?` 用法气泡当绝对定位锚点
  - **代价与取舍**（都是实测出来的，不是拍脑袋）：
    - 搜索框宽度必须封顶 280px（原来是 `flex:1` 通栏）。**临界点实测**：1280 窗口 + OCR 进度条可见时，340px 会把「标注输入框显示开关」挤到第 3 行（工具栏 109→152px，卡片区反少 43px），280px 正好收得下
    - 280px 装不下原占位符「字符 / U+4E2D / 1-50（空格 AND 组合）」（被截成「1-50（空」），已缩为「字符 / U+4E2D / 1-50」——「空格 AND 组合」的说明本来就在 `?` 气泡里
    - OCR 进度条上限 320→130px、进度文字 `min-width` 90→76px（同理，OCR/导出时那一行要同时放下搜索框和繁简切换）
    - `.char-count-info` **必须去掉 `margin-left: auto`**：工具栏里 `.mode-toggle` 已有一个 auto，两个 auto 把剩余空间对半分，字符数会被推到行中间、与搜索框分家
  - 删掉随之作废的两条死 CSS：`.action-buttons`、`.action-btn`（后者是上一条搬按钮时留下的，当时按「死代码只提不删」保留；本轮它的兄弟规则也要删，一并清掉）
  - **验证**（Playwright 实测 10 项，全部通过）：搜索「决」→1 张、「1-5」→5 张、Esc 清空→741 张；`/` 键聚焦到搜索框；`?` 气泡落在搜索栏正下方 4px 且在视口内、点外部关闭；滚动 800px 后搜索栏位置不变；字符计数随输入更新；输入框开关收起后卡片净高 756px；OCR 阈值滑块显示 0.80；工具栏 109px / 卡片净高 623px
  - 涉及文件：`templates/annotate.html`（DOM 重组 + 4 处 CSS）

- **/annotate 搜索栏紧贴操作栏，消除两者之间的空隙**（页面4-标注出图）
  - 现象：搜索栏（`.card-search-bar`）与上面的操作栏（`.action-buttons`）之间空出 20px 灰底，且搜索栏比操作栏窄 40px（两侧各缩进 20px），看起来是两段割裂的条
  - 根因：搜索栏在 `.card-grid-container` 内，而容器有 `padding: 20px`
  - 修复：容器改 `padding: 0 20px 20px`（去掉上内边距），搜索栏改 `margin: 0 -20px 20px`（左右负边距抵消容器内边距 → 与操作栏一样通栏），`.card-grid` 改 `margin: 20px auto 0`（搜索栏隐藏时也要与上方留 20px；可见时与搜索栏的下边距折叠，仍是 20px）
  - **踩坑记录**：一开始想用 `margin-top: -20px` 把搜索栏拉上去，没生效——**`position: sticky` 的元素上外边距无效**（实测 -20px/-60px 都推不动，把 position 改成 static 后立刻生效）。所以只能从容器的内边距让位
  - 验证：空隙 0、搜索栏宽 1280 = 操作栏宽、与卡片间距仍 20px；滚动 400px 后搜索栏仍吸附在操作栏下（sticky 未受影响）
  - 涉及文件：`templates/annotate.html`（3 处 CSS）

- **新增命令行工具 `scripts/build_personal_ttf.py`：把导出的字形图打包成 TTF 字体**（工具，非页面功能）
  - 背景：此前给 ai-font-tool 合成 REF_FONT 用过一次性脚本（路径写死、依赖未声明、只在本机跑过一次）。现整理成可复用的命令行工具
  - **用法**：`uv sync --extra ttf` 装可选依赖 → `python scripts/build_personal_ttf.py --session <会话hash> --out <字体路径>`；也可用 `--export-dir` 指向任意目录。字体名/度量/图像极性（白底黑字 or 黑底白字，默认自动判断）都可参数化，`-h` 看全部
  - **实现改用 fontTools 官方 `FontBuilder` 从零建字，不再需要模板字体**（旧版拿 ai-font-tool 的 default.ttf 当骨架、跨项目耦合）；`fonttools` 声明为可选依赖 `[project.optional-dependencies].ttf`，不污染主依赖（网页应用本身不碰字体文件）
  - **修掉旧版两个度量缺陷**（都是拿真实字体量出来的）：
    - **advance**：原按「墨迹宽度」设置，而墨迹却居中在 1843 宽的绘制框里——实测 advance 1317 而墨迹跨 274..1569，字会溢出自己的步进框、文本排版时重叠。改为 **advance = 整个 em 框**（CJK 字体标准：每字一个等宽方格）
    - **hmtx 的 lsb**：原写成 0，而 FreeType/PIL 是按 lsb 定位的 → 字形整体左移 xMin（实测偏 37~125px）。改为 **lsb = xMin**（TrueType 惯例）
  - 写出后自动回读校验（字形数 / cmap 码点数 / 字形名 / 度量 / lsb==xMin）并打印结果，不必手工再验一遍
  - **验证**：152 个字形全部通过校验；抽查 20 个字形用 PIL 实际渲染，墨迹左边界与字体 xMin 完全一致（与对照字体 default.ttf 的行为相同）
  - **已知限制**（写在脚本 docstring 里）：轮廓是「按列矩形拼接」，放大看呈阶梯状（改用 `cv2.findContours` + `approxPolyDP` 可拿到真轮廓且文件更小，项目已依赖 OpenCV，属可做的改进）
  - 顺带在 CLAUDE.md 记了一条环境警告：**`uv sync` 会卸载「未在 pyproject 声明的包」**——本机为 GPU 加速手动装的 `onnxruntime-gpu==1.18.1` 会被它删掉（且它与 CPU 版 `onnxruntime` 抢同一批文件），误删后用 `uv pip install "onnxruntime-gpu==1.18.1"` 装回
  - 涉及文件：`scripts/build_personal_ttf.py`（新增）、`pyproject.toml`（可选依赖）、`CLAUDE.md`（结构 + 常用命令 + 环境警告）；删除旧的根目录一次性脚本 `build_personal_ttf.py`

- **/annotate 工具栏整合 + 标注输入框可收起**（页面4-标注出图）
  - 需求：① `input-section`（繁简输入区）用一个按钮控制展开/收起，按钮名「标注输入框显示开关」；② `action-buttons` 里的按钮并入 `toolbar`，让页面更紧凑
  - **① 输入区可收起**：`input-section` 加 `id="inputSection"`；收起态由 CSS 类 `.collapsed { display: none }` 控制，工具栏末尾新增「标注输入框显示开关」按钮切换。按钮自身用 `.active` 表示「输入区当前可见」（与 `.mode-btn.active` 同一套表意）。**默认展开**，页面初始状态与改动前一致
  - **② 按钮并入工具栏**：5 个按钮（开始标注 / OCR 自动标注 / 只看待复查 / 保存标注 / 清空）从 `.action-buttons` 移入 `.toolbar`，插在「切割布局」与繁简切换之间（繁简切换有 `margin-left:auto` 会把自身及之后的元素推到右侧）；基础类 `action-btn` → `toolbar-btn` 以统一内边距与字号，配色类（`btn-annotate` / `btn-ocr` / `btn-save-annot` / `btn-clear` / `ocr-filter-btn`）原样不变
  - **非按钮项按原样留在 `.action-buttons`**：OCR 阈值滑块 + 进度条、字符数/卡片数——这一行从 44px 缩成 **38px** 的细状态条
  - 顺带修正 OCR 按钮的 title（原写 EasyOCR，实际引擎早已换成 RapidOCR）
  - 遗留死 CSS：`.action-btn` 规则现已无人使用，按「发现死代码只提一句、不删」保留
  - 验证（Playwright 实测）：开关三态（展开 → 收起 → 展开，`.collapsed` 与 `.active` 同步变化）；工具栏 15 个按钮齐全；并入后的「清空」功能正常、输入文字后「开始标注」正确启用；控制台 0 错误
  - 涉及文件：`templates/annotate.html`（DOM 重组 + CSS）、`static/js/annotate.js`（元素引用 + 开关绑定）；annotate.js 版本号 v30 → v31

- **/annotate 新增「放缩」：右键卡片可单张调整字形的尺寸与位置**（页面4-标注出图）
  - 需求：右键菜单加第三项「放缩」，弹窗里用八个手柄自由缩放字形，并带「按比例」勾选框
  - **交互**：手柄框住的是**字形墨迹范围**（不是整张 512 图）——向外拖放大、向内拖缩小；框内按住可整体平移；勾「按比例」（默认勾上）时拖左右以宽为准推高、纯拖上下边以高为准推宽，保持打开时的宽高比不变形
  - **画布设计**：640×640，512 字图居中画在偏移 (64,64)、1:1 不缩放；四周留 64px 是为了让手柄能拖到 512 格子**外面**——否则放开大时用户看不见「哪一部分会被裁掉」
  - **越界提示**：矩形落在 512 格子外的部分在画布上涂红，数值行同时提示「越界部分会被裁掉」
  - **手柄实现沿用页面1 绿框那套范式**（`layout.js` 的 8 方向枚举 + 统一命中阈值 + 光标映射），但画布是固定 1:1，去掉了 `zoomLevel` 换算；`annotate.js` 是独立文件、加载不到 `layout.js`，故按同样写法自带一份
  - **新增后端接口 `POST /api/rescale_char/<hash>`**：按目标矩形重贴字形，**原地覆写同名文件**
    - 墨迹包围盒用纯 >127 阈值求，**刻意不用** `detect_char_bbox()`——它会先做 3×3 腐蚀（那是自动流水线去边缘噪点用的），手动放缩跟着腐蚀会把贴边笔画裁掉 1~2px；与前端口径一致 → 所见即所得
    - 缩小用 `INTER_AREA`、放大用 `INTER_LINEAR`；目标矩形与画布**求交后写入**，负坐标与越界自然裁掉
    - **首次操作前自动备份**为 `scaled_NNNN.bak.png`，已存在则不覆盖（始终保留最原始那一版，便于一键回到「缩放校正」的产出）
    - 文件名不变 → `ocr_annotations.json`、`cutting.json`、导出全链路不受影响（它们都不读图片尺寸）
  - **为什么必须新开接口**：`/api/process_scale` 的输出名是 `scaled_{i:04d}.png`，`i` 是传入数组的枚举下标——只传一张会写成 `scaled_0000.png` 覆盖别人的图
  - **验证**（Playwright 实测）：右键菜单三项且「放缩」第三 ✓；弹层渲染（墨迹框 x42 y48 宽427 高415 与文件实际吻合）✓；按比例拖右侧中点 +100px → 宽 427→527、高自动 415→512.19、比例 1.029 保持不变 ✓；取消勾选后同样的拖动 → 宽变高不变（自由缩放）✓；框内拖动 → 整体平移 50/-50、宽高不变 ✓；重置 → 精确回到初始框 ✓；确定 → 越界裁到 420×413 @ (92,0)、备份与原始逐字节一致、卡片图换新时间戳刷新 ✓；服务端边界（负坐标/越界/路径穿越/矩形过小/图不存在/完全在画布外）全部按预期拒绝或裁剪 ✓
  - 涉及文件：`templates/annotate.html`（弹层 DOM + CSS）、`static/js/annotate.js`（菜单项 + 放缩逻辑）、`app.py`（`rescale_char`）；annotate.js 版本号 v29 → v30

- **`scale_char` 支持角对齐（`top-left` / `top-right` / `bottom-left` / `bottom-right`）**（页面3-缩放校正）
  - 背景：标点符号在字库里不能居中——「；」要靠左下角、「“」靠右上角、「”」靠左上角，居中不符合排版惯例
  - 改动：`align` 新增四个角对齐取值。贴边留白沿用 `top` / `baseline` 原本就在用的 `max(5, 2% 画布宽)`（512 → 10px）；原 `center` / `top` / `baseline` 的放置逻辑与取值完全不变
  - **回归验证**：改动前后 `align=center` 的输出**逐字节相同**（最大像素差 0），即对现有调用零影响
  - 说明：`/scale` 页面暂未暴露这四个取值（仍只有 居中/上/下 三个单选），本次是为了在命令行/脚本里批量修标点；`ALGORITHM_VERSION` 保持 2（现有行为未变，不需要触发全量重跑）
  - 涉及文件：`utils/scale_processor.py`（`scale_char` 放置逻辑 + docstring）

- **「绿色框设置」新增「按距离合并绿框」**（页面1-切割布局）
  - 需求：有些字被检测成几块（笔画断开、印章与正文分家），希望把「挨得近」的绿框自动并成一个
  - 实现：模态框底部新增合并区块——距离阈值滑块（0–50px，默认 5）+ 间距分布直方图 + 「按距离合并绿框」按钮
  - **间距定义**：两框的边缘间距（矩形最接近处的直线距离）——x/y 方向分别求间隙、重叠方向记 0，再取欧氏距离。交叠/包含的框间距恒为 0，任何阈值下都参与合并（用户明确要求）
  - **成组用连通分量**（并查集）：A-B 近、B-C 近 → 三个并成一组，避免为链式相邻的碎片反复点按钮
  - **合并动作复用 performMerge 那套**：新框 = 组内所有框的最小包围盒，再 syncBoxDerived + clampBoxToImage，删旧框加新框；撤销栈同样在合并前压一次
  - **与过滤解耦**（用户明确要求）：本按钮只改绿框集合，不读不写不触发任何过滤条件；但会同步绿框主集 `masterBoxes`——否则合并后再点「应用」过滤会从「未合并的主集」重建 `state.boxes`，合并白做
  - **直方图**：横轴 = 边缘间距 0–50px（线性，每格 2px），纵轴 = 落在该区间的**框对数量**（合并不是逐框判断而是成对判断，框对数才是调阈值时真正关心的量）；阈值左侧高亮，橙色竖线可拖动选阈值（做了 CSS 缩放换算，画布 `width:100%` 时也准）
  - **框对缓存「数组实例 + 坐标签名」双校验**：O(n²) 建表（1379 框实测 99ms）需缓存，过期判定用双校验而非让调用方手动失效——绿框会被识别/拖动/缩放/删除/过滤/撤销反复改动，靠调用点逐个清缓存迟早漏一个。两个校验缺一不可：
    - 只比坐标签名 → **踩过的坑**：撤销（`restoreSnapshot`）与过滤会把 `state.boxes` 换成「坐标完全相同的深拷贝」，签名一致但缓存里的 `{a,b}` 仍指向旧对象 → 合并时按引用一个都查不到 → 静默不合并。真实数据上表现为「阈值调大反而一个都不并」（阈值 0 并掉 337 个，阈值 2/5/10/20/50 全是一个都不并）
    - 只比数组实例 → 拖动/缩放是原地改坐标，实例不变 → 间距已变却仍用旧缓存
  - **真实数据实测**（1379 个绿框）：阈值 0→1042 框、2→953、5→859、10→791、20→733、**50→114**。注意阈值 50 会把 1379 个框连锁并成 114 个，调阈值务必看 toast 的「绿框 N → M 个」与直方图，别拉太高
  - **模态框高度改为按内容自适应**：原先固定 453px，加入合并区块后内容高达 635px，合并区块被挤到折叠线以下、不滚动看不到。改为「先定宽度与位置，再量-设-再量迭代收敛」算出高度，上限为可视区高度（真放不下时才滚动）。踩坑记录：**必须先设宽度再量高度**——宽度决定文字换行，先量后设宽度会量到「另一个宽度下」的内容高度，宽度一设完就溢出
  - 涉及文件：`templates/layout.html`（合并区块 + 操作说明）、`static/css/style.css`（`.merge-panel`）、`static/js/layout.js`（`boxEdgeGap` / `boxesSignature` / `getMergePairs` / `renderMergeDistanceHistogram` / `initMergeDistHistogramDrag` / `mergeGreenBoxesByDistance` / `openGreenBoxModal` 高度自适应）；资源版本号 v22 → v26

- **页面1 新增「加载切割线」按钮**（页面1-切割布局）
  - 需求：在「存储切割线」下面加一个反向操作按钮，从磁盘读回配置
  - 实现：`存储切割线` / `加载切割线` 竖排成一对（新增 `.panel-actions-col` 竖排容器 CSS）；加载走已有接口 `/api/load_session/<hash>`，读取 `data/sessions/<hash>/cutting.json` 的 `vertical_lines` / `horizontal_lines` / `strip_horizontal_lines` / `boxes` 覆盖到 `state`，并同步绿框主集 `masterBoxes`（否则「重置绿框过滤」会用旧主集还原出错集合）
  - 撤销保护：加载前 `pushHistory()`，误点可用撤销按钮还原画布；接口失败时只提示错误，不覆盖画布现有数据
  - 悬停提示：title 写明「在相应的 session id 里面寻找 cutting.json（data/sessions/<session id>/cutting.json）」
  - 涉及文件：`templates/layout.html`（按钮 + 竖排结构）、`static/css/style.css`（`.panel-actions-col`）、`static/js/layout.js`（`loadCutLines` + 绑定 + 启用条件）；资源版本号 v21 → v22

### Bug修复

- **短屏下「绿色框设置」底部内容被裁掉、按钮点不到**（页面1-切割布局）
  - 现象：内容超出模态框高度时**没有滚动条**，底部的「距离阈值」滑块与「按距离合并绿框」按钮被直接裁在可视区外——720p 高的窗口下完全点不到
  - 原因：`static/css/style.css` 里有**两处 `.modal-body` 定义**。后面「调整弹窗」那组里的 `overflow: hidden` 因为选择器同权重、又出现在后面，盖掉了前面那条的 `overflow-y: auto`。而那条规则对「调整弹窗」本身是**失效规则**——那个弹窗的结构是 `.modal-content > .preview-area + .modal-sidebar`，压根没有 `.modal-body` 元素，属复制粘贴残留
  - 修复：加 `#greenBoxModal .modal-body { overflow-y: auto }`（ID 提高优先级）把滚动加回来，注释里写明冲突来源
  - 验证：720p 下滚到底后按钮完整可见且可点击（点一次绿框 4→2）；1600×1000 内容放得下时不出现多余滚动条；另两个模态框（编辑绿框/快捷键设置）内容未溢出、不受影响
  - 遗留：调整弹窗那组里的 `.modal-body` 是失效规则，建议后续清理（本次按「发现死代码只提一句、不删」处理）
  - 涉及文件：`static/css/style.css`；资源版本号 v23 → v24

- **撤回/重做不含绿框主集，导致「撤销被应用抵消」**（页面1-切割布局）
  - 现象：绿框按距离合并后点「撤回」——画布上的绿框正确恢复，但打开「绿色框设置」发现预览统计仍是合并后的数目；此时点「应用」，绿框又变回合并态，**刚做的撤销被抵消**
  - 原因：`snapshotState()` / `restoreSnapshot()` 只覆盖 `boxes` 等字段，**没有 `masterBoxes`**，而识别、按距离合并、标记绿框、Alt 删除都会改主集。过滤是「`state.boxes = masterBoxes.filter(...)`」——主集不回滚，撤销后再过滤就把旧状态拉回来了
  - 修复：快照纳入 `masterBoxes`（深拷贝）。这样识别、合并、标记、删除等所有改主集的操作，撤销/重做都能连主集一起回滚
  - 代价：每步快照多深拷贝一份主集（1379 个绿框约每步 +275KB，历史上限 50 步 → 峰值多约 13MB 内存）
  - 涉及文件：`static/js/layout.js`（`snapshotState` / `restoreSnapshot`）

- **Ctrl+Z / Ctrl+Y 快捷键实际未实现**（页面1-切割布局）
  - 现象：撤回/重做按钮的 title 写着「(Ctrl+Z)」/「(Ctrl+Y)」，但按了没反应
  - 原因：layout 页唯一的 keydown 处理器只处理 Delete/Backspace，从未注册过撤销/重做快捷键
  - 修复：在既有 keydown 处理器里补 `Ctrl+Z` 撤销、`Ctrl+Y` 与 `Ctrl+Shift+Z` 重做。焦点在 input/textarea 时不拦截——输入框里的 Ctrl+Z 交给浏览器（撤掉刚打的字），避免与用户预期打架
  - 涉及文件：`static/js/layout.js`（keydown 处理器）

